import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:layout/ui/product_grid.dart';
import 'package:layout/utils/providers.dart';
import 'package:mockito/mockito.dart';
import 'package:provider/provider.dart';



/*Widget createProductGridScreen() => ChangeNotifierProvider<CartItemCounter>(
  create: (context) => ProductGrid(myCategory: ''),

  child: const MaterialApp(
    home: ProductGrid(myCategory: 'electronics'),

  ),
);*/

void main() {
 /* group('Product Grid Tests', () {
    testWidgets('Scroll Test', (WidgetTester tester) async{
      await tester.pumpWidget(MultiProvider(
          providers: [
            Provider<CartItemCounter>(
                create: (_) => ProductGrid(myCategory: 'electronics'))
          ]));
    });


  });*/
}


